
############################################################
##     UNCOMMENT ANY TARGET TO OVERWRITE THE DEFAULT!     ##
############################################################

# AR     := # Create static libraries (default: ar)
# AS     := # Compile assembly        (default: nasm)
 CC     := clang # Compile C               (default: gcc)
# FC     := # Compile C++             (default: gfortran)
# CXX    := clang # Compile Fortran         (default: g++)
# RANLIB := # Update static libraries (default: ranlib)
